# 埼玉大学宇宙工学サークルあかとき Website
このリポジトリは埼玉大学宇宙工学サークルあかときのWebsiteを管理するリポジトリです。GitHub Pagesで公開しています。

https://akatoki-saidai.github.io/

## Development
このリポジトリでは、[Astro](https://astro.build/)というWebフレームワークを使用しています。ローカルで開発を行うには、以下のツールが必要です。

- **Git** : ソースコードの管理に使用します。
- **mise** : Node.jsとpnpmのインストールに使用します。
- **Node.js (`v24.x`)** : JavaScriptの実行環境です。
- **pnpm (`v10.x`)** : JavaScriptのパッケージマネージャーです。

### 初期セットアップ
> [!NOTE]
> セットアップの内容は一例なので、同じ環境を構築できる場合は以下のセットアップ内容に従わなくても問題ないです。

以下のセットアップはWindows 11環境を想定しています。
#### Visual Studio Codeのインストール
https://code.visualstudio.com/ からVisual Studio Code (VSCode) をインストールします。他のエディタを使用する場合はインストールを省略して問題ないです。ファイル編集作業にVSCodeを使用します。
#### Gitのインストール
https://git-scm.com/ からGitというファイル差分管理ツールをインストールします。GitとGitHubは別物なので注意してください。インストーラーは基本的にデフォルト設定でインストールすれば問題ないです。インストールが完了したら、PowerShellを開いて以下のコマンドを実行します。バージョン情報が表示されればGitのインストール完了です。
```
git --version
```
編集作業を行うときに、usernameとemailが必要になるので、登録します。この時、GitHubに登録したユーザー名とメールアドレスで登録します。以下の""内を編集して実行します。
```
git config --global user.name "username"
git config --global user.email "emailaddr@gmail.com"
```
#### miseのインストール
PowerShellを開いて、以下を実行します。
```
winget install jdx.mise
```
上記のコマンドを実行後に以下のコマンドを実行します。
```
if (!(Test-Path $PROFILE)) { New-Item -ItemType File -Path $PROFILE -Force }; Add-Content -Path $PROFILE -Value 'mise activate pwsh | Out-String | Invoke-Expression'
```
PowerShellを開きなおして、以下を実行します。バージョン情報が表示されれば`mise`のインストール完了です。
```
mise --version
```
#### GitHubリポジトリのクローン
GitHubのリポジトリをクローンして、ローカルで編集作業が出来るようにします。クローンしたリポジトリを置いておく場所を用意します。（`C:\User\Username\GitHub`などをエクスプローラで作成）そのフォルダをVSCodeで開きます。`Ctrl+@`でPowerShellが開くので、そこで以下のコマンドを実行します。
```
git clone https://github.com/Akatoki-Saidai/Akatoki-Saidai.github.io.git
```
続いて、以下のコマンドを実行します。VSCodeの新しいウィンドウが開きます。
```
code Akatoki-Saidai.github.io
```
#### Node.jsとpnpmのインストール
> [!TIP]
> 今まで`Node.js`と`npm`を使用していた場合は、`npm install -g pnpm`で`pnpm`を使用することができます。

VSCodeで`Akatoki-Saidai.github.io`のフォルダを開いている状態で、以下のコマンドを実行します。`mise.toml`を参照して`Node.js`と`pnpm`がインストールされます。
```
mise install
```
以下のコマンドを実行して、それぞれのバージョンが表示されれば完了です。
```
node --version
pnpm --version
```
#### 開発サーバーの立ち上げ
以下のコマンドを実行して、依存関係をインストールします。
```
pnpm install
```
続いて、以下のコマンドを実行します。
```
pnpm dev
```
開発サーバーが立ち上がるので、その状態で http://localhost:4321/ にブラウザでアクセスします。この状態でファイルを編集すると、開発サーバーに変更が反映されるので、表示を確認しながら編集作業を行うことができます。開発サーバーを終了するには、`q`を入力するか`Ctrl+C`で終了することができます。開発サーバーをまた立ち上げる場合は、`pnpm dev`で立ち上がります。

### 開発の流れ
> [!IMPORTANT]
> 以下の説明では簡単のため`main`ブランチで作業を行っていますが、本来は編集を行う単位ごとにブランチを分けるのが適切です。運用としては、そのときのWebsite管理者にお任せします。

まず初めに、これから編集作業を行う場合はGitHubから最新版を引っ張ってくる必要があります。以下のコマンドを実行します。
```
git pull
```
または
```
git pull origin main
```
ローカルのリポジトリが最新になったら、編集作業を行います。（`pnpm dev`で開発サーバーを立ち上げたりしながら編集を行う）編集作業に一区切りついたら、以下のコマンドを実行して、変更をステージングします。
```
git add .
```
その後、以下のコマンドを実行してコミットします。コミットメッセージは""内に変更内容についてわかりやすいように書きます。`git log --oneline`で過去のコミットメッセージを参照できます。
```
git commit -m "コミットメッセージ"
```
> [!CAUTION]
以下のコマンドを実行することでGitHubに変更がアップロードされます。`main`ブランチで作業した内容がGitHubに上がると、自動的にWebsiteに変更した内容が反映されてしまうので、十分注意してください。

```
git push
```
または
```
git push origin main
```

### ディレクトリ構成
このリポジトリのディレクトリ構成は大まかに以下のようになっています。ただし、開発時のローカルには`.astro`, `.git`, `node_modules`などが存在します。（`.gitignore`で無視されるので、リモートリポジトリにはpushされません）
```
./
├── .github/         # GitHub Actionsの設定
├── .vscode/         # VSCodeの設定
├── public/          # 静的アセットを格納するディレクトリ
├── src/             # ソースコードを格納するディレクトリ
│   ├── components/  # 再利用可能なコンポーネントを格納
│   ├── layouts/     # サイト全体のレイアウトテンプレートを格納
│   ├── pages/       # サイトの各ページファイルを格納
│   ├── styles/      # サイトのCSSファイルを格納
│   └── env.d.ts     # TypeScriptの型定義ファイル
├── .gitignore       # Gitの管理対象に含めないファイルの定義
├── .nojekyll        # Jekyllのビルドプロセスをスキップ
├── astro.config.mjs # Astroの設定ファイル
├── mise.toml        # miseの設定ファイル
├── package.json     # 依存関係やスクリプトの定義ファイル
├── pnpm-lock.yaml   # pnpmのロックファイル
├── README.md        # 今見ているファイル
└── tsconfig.json    # TypeScriptの設定ファイル
```
#### 📁 `public/`
ブラウザから直接アクセス可能な静的ファイルを配置します。（画像、PDFなど）
> [!NOTE]
> このディレクトリに配置された静的ファイルは、ビルドプロセスなどを通らずに、そのまま公開されます。
- `bg/`
  - Websiteの背景に使われている画像が格納されています。
- `img/`
  - Websiteで使用する画像が格納されています。
- `photos/`
  - 写真一覧ページの写真が格納されています。写真が追加されると、そのまま写真一覧ページに表示されます。
- `.`
  - ロゴやファビコン、会則・実験規約書・安全講習などのファイルも配置されています。
#### 📁 `src/`
Websiteのメインのソースコードが格納されています。
##### 📁 `components/`
複数のページで使われるUIパーツが格納されています。ヘッダーやフッターの変更を行う場合はこれらのファイルを編集します。
- `Footer.astro`
  - 共通のフッターコンポーネント
- `Header.astro`
  - 共通のヘッダーコンポーネント
##### 📁 `layouts/`
ページの共通レイアウトが格納されています。ヘッダーやフッターはこのレイアウトファイルにすでに含まれています。共通レイアウトを変更する場合は、ここに格納されているファイルまたは、コンポーネント単位の編集であればコンポーネントファイルを編集します。
> [!NOTE]
> 新しくページを作成するときは、すでにあるものと同様にレイアウトファイルを参照するようにします。
> 
> `src/pages/index.astro`
> ```astro
> ---
> import Layout from "../layouts/Layout.astro";
> ---
> ```

- `Layout.astro`
  - Webページのレイアウトファイル（`pages/*.astro`ではこのレイアウトファイルを参照するようにする）
- `Markdown.astro`
  - Markdown記事の場合は、このレイアウトファイルを参照するようにします。
##### 📁 `pages/`
Webページのファイルが格納されています。
> [!IMPORTANT]
> `src/pages/`内のファイル構成がそのままサイトのURLになります。
> - `index.astro` -> `/`
> - `about.astro` -> `/about`
> - `posts/safety.md` -> `/posts/safety`

- `posts/`
  - Markdownの記事を格納するディレクトリ
- `activity.astro`
  - 団体概要ページのファイル
- `article.astro`
  - 記事一覧ページのファイル
- `form.astro`
  - 入会・継続フォームページのファイル
  - [Google Form更新時の対応（参考ブログ）](https://zenn.dev/yurukei20/articles/9741118bfb5ee0)
- `index.astro`
  - ホームページのファイル
- `photos.astro`
  - 写真ページのファイル
- `shinkan.astro`
  - 新歓情報ページのファイル
- `thankyou.astro`
  - フォーム送信後のthankyouページのファイル
##### 📁 `styles/`
すべてのスタイルファイルをここに格納しています。
> [!NOTE]
> `global.css`, `markdown.css`以外は基本的にクラスセレクタでCSSを適用しています。

- `global.css`
  - `layout.astro`を参照して作成するページにすべて適用されるCSS
- `markdown.css`
  - `markdown.astro`を参照して作成するページ（特にMarkdown記事）にすべて適用されるCSS
- `activity.css`
  - `activity.astro`に適用されるCSS
- `article.css`
  - `article.astro`に適用されるCSS
- `form.css`
  - `form.astro`に適用されるCSS
- `index.css`
  - `index.astro`に適用されるCSS
- `photos.css`
  - `photos.astro`に適用されるCSS
- `shinkan.css`
  - `shinkan.astro`に適用されるCSS
- `thankyou.css`
  - `thankyou.astro`に適用されるCSS

#### `.github/workflows/deploy.yml`
このファイルは、GitHub Pagesにリポジトリのビルド成果物をデプロイするGitHub Actionsのyamlファイルです。https://github.com/withastro/action に最新版が置かれているので、GitHub Actionsのビルドが通らなくなったりした場合は、最新版に更新してください。
